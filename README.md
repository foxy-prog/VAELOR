# VAELOR
VÆLOR’s Authorization &amp; Capability Engine controls what an authenticated identity can execute. It evaluates identity, device trust, session, capability, resource, context, risk, autonomy, and Governance policy, returning ALLOW, DENY, or REQUIRE_APPROVAL. It enforces least privilege, default-deny security, approvals, revocation, and audit decisions.
